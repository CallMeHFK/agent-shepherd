## Summary

<!-- What does this PR change, and why? -->

## Affected area

<!-- qwenpaw / claude / codex / core (server, detectors, judge, ledger) / docs / other -->

## How it was tested

<!-- e.g. `uv run pytest -q`, live agent run, `shepherd replay` of a session -->

## Checklist

- [ ] `uv run pytest -q` passes locally
- [ ] `uv run ruff check .` passes locally
- [ ] No real API keys or secrets added (placeholders / dummy values only)
- [ ] README updated if user-facing behavior changed
